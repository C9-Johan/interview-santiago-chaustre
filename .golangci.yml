version: "2"
run:
  tests: true
linters:
  enable:
    - bodyclose
    - cyclop
    - dupl
    - errcheck
    - errorlint
    - funlen
    - gocognit
    - goconst
    - gocritic
    - gosec
    - govet
    - maintidx
    - nestif
    - prealloc
    - revive
    - staticcheck
  settings:
    cyclop:
      max-complexity: 30
      package-average: 10
    dupl:
      threshold: 150
    funlen:
      lines: 100
      statements: 50
    gocognit:
      min-complexity: 20
    goconst:
      min-len: 3
      min-occurrences: 3
    gocritic:
      enabled-checks:
        - hugeParam
        - rangeValCopy
      settings:
        hugeParam:
          sizeThreshold: 256
        rangeValCopy:
          sizeThreshold: 256
    maintidx:
      under: 20
    nestif:
      min-complexity: 5
    revive:
      rules:
        - name: unused-parameter
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - cyclop
          - dupl
          - funlen
          - gocognit
          - maintidx
        path: _test\.go
      - linters:
          - errcheck
          - revive
          - staticcheck
        path: mocks/
    paths:
      - third_party$
      - builtin$
      - examples$
      - mocks
formatters:
  enable:
    - gofumpt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
      - mocks
  settings:
    goimports:
      local-prefixes: github.com/chaustre/inquiryiq
